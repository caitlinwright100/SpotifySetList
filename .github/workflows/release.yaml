on:
  workflow_dispatch:
    inputs:
      environment:
        required: true
        type: choice
        options:
        - dev
        - preprod
        - prod

jobs:
  log-the-inputs:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Environment: $ENVIRONMENT"
          echo "This is the hash: $SHA"
        env:
          ENVIRONMENT: ${{ inputs.environment }}
          SHA: ${{ github.sha }}

      - name: "Create new launch template version"
        run: |
          aws ec2 create-launch-template-version \
          --launch-template-id your-launch-template-id \
          --source-version your-existing-version-number \
          --launch-template-data '{"UserData":"${{ github.sha }}"}'

      - name: "Instance refresh"
        run: |
            #  Code for cailtin to write
      